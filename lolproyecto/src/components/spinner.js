import React from 'react'
import '../styles/spinner.css'

const spinner = () => {
    return (
        <div>
            <div class="loader">Cargando...</div>
        </div>
    )
}

export default spinner
